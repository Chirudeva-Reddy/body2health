# Configuration Files

This directory stores YAML/configuration files only.

Current layout:

```text
configs/
  default.yaml
  segmentation/
    sam2.1/
      sam2.1_hiera_l.yaml
```

Binary model weights belong under `models/`, not here.
