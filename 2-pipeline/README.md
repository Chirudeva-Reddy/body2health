# 2-pipeline

Numbered placeholder for the iPhone preprocessing pipeline. The actual Python
package lives at the repo root as `pipeline/` so it is importable with the
canonical name:

```python
from pipeline.iphone_pipeline import process_iphone_image
```

This directory exists only so `ls` at the repo root reads in pipeline order.
Do not put code here.
